<!DOCTYPE html>
<html>
    <head>
        <title>TV &amp; iPlayer branding service</title>
        <link rel="stylesheet" href="http://static.bbci.co.uk/frameworks/barlesque/2.60.9/orb/4/style/orb.css" />
        <style>
        body {
            margin: 36px;
        }
        .branding-service {
            font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
            font-weight: 300;
        }
        h1.branding-service {
            font-size: 16px;
        }
        h2.branding-service {
            font-size: 24px;
            line-height: 1.6;
            margin-bottom: 36px;
        }
        h2.branding-service a {
            color: black;
            text-decoration: none;
            position: relative;
            display: inline-block;
            -webkit-perspective: 600px;
            -webkit-perspective-origin: 50% 100%;
            -webkit-transition: color 0.3s;
        }
        h2.branding-service a:before {
            background-color: #f54997;
            -webkit-transition: -webkit-transform 0.2s;
            -webkit-transform-origin: 50% 100%;
            -webkit-transform: rotateX(90deg);
            transition-timing-function: cubic-bezier(0.7,0,0.3,1);
            position: absolute;
            top: 0;
            left: -4px;
            z-index: -1;
            padding: 0 4px;
            width: 100%;
            height: 100%;
            content: '';
        }
        h2.branding-service a:after {
            border-bottom: 1px solid #f54997;
            position: absolute;
            top: 0;
            left: -4px;
            z-index: -1;
            padding: 0 4px;
            width: 100%;
            height: 100%;
            content: '';
        }
        h2.branding-service a:hover:before {
            -webkit-transform: rotateX(0deg);
        }
        h2.branding-service a:hover {
            color: white;
        }
        hr.branding-service {
            background: rgba(0, 0, 0, 0.25);
            height: 1px;
            border: 0;
            margin: 36px 0 0;
        }
        </style>
    </head>
    <body>
        <h1 class="branding-service">TV &amp; iPlayer branding service</h1>
        <div id="tviplayer">
            {{{ content }}}

            <style>
                {{{ css }}}
            </style>
        </div>
    </body>
</html>